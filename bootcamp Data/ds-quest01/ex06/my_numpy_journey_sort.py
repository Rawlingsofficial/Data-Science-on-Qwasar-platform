import numpy as np

def my_numpy_journey_sort(param_1):
    # Convert the standard array to a NumPy array
    numpy_array = np.array(param_1)

    # Sort the NumPy array
    sorted_array = np.sort(numpy_array)

    # Return the sorted NumPy array
    return sorted_array
